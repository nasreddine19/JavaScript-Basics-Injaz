/**
 * Exercise 1: Primitive Types
    Problem:
        1- Create the following variables with appropriate primitive types:
            - A variable age to store your age as a number.
            - A variable userName to store your name as a string.
            - A variable isStudent to store whether you are a student or not as a boolean.
            - A variable bigNumber to store a very large number (e.g., 9007199254740992) as a BigInt.
            - A variable uniqueId to store a unique identifier as a Symbol.

        2- Log all the variables to the console.
        3- Perform the following operations:
            - Change the value of age and log it again.
            - Create a new variable copyUserName and assign it the value of userName. Then, change the value of 
              userName and log both userName and copyUserName to see if they are the same.
 */

/**
 * Exercise 2: Reference Types
    Problem:
        1- Create an object `profile` with the following properties:
            - userName: A string (e.g., "Nasreddine").
            - age: A number (e.g., 22).
            - hobbies: An array of strings (e.g., ["reading", "coding"]).
        
        2- Log the profile object to the console.
        3- Perform the following operations:
            - Create a new variable friend and assign it the value of profile.
            - Change the userName property of friend to "Alex".
            - Log both profile and friend to the console to see if they are the same.

        4- Create a new object cloneProfile that is a copy of profile but not linked to it. 
        Change a property in cloneProfile and log both profile and cloneProfile to confirm they are independent.
 */