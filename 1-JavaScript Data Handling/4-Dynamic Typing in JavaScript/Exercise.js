/**
 * Create a variable called myData and assign it a number (e.g., 100). 
 * Then, change its value to a string (e.g., "JavaScript"). 
 * Print the value of myData each time using console.log().
 */