/**
 * Template literals allow you to create strings with embedded expressions. 
 * Use backticks (``) instead of quotes.
 */

// Example:

let firstName = 'Nasreddine'
let lastName = 'BOUDELLAL'

let fullName = `${firstName} ${lastName}`
console.log(fullName)