/**
 * The Conditional ternary operator is a shortcut for an `if-else` statement. 
   It has three parts:
    1- A condition
    2- A value if the condition is true
    3- A value if the condition is false
 */

    
let age = 21
let isAdult = age >= 19 ? 'Yes' : 'No' // isAdult is 'Yes'