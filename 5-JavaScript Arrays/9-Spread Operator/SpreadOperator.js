/**
 * Spread Operator
 * The spread operator (...) takes all items from an array or object and spreads them out.
 */

const numbers = [1,2,3,4,5];
const newNumbers= [...numbers]; // Copies the array
console.log(newNumbers); // [1,2,3,4,5]