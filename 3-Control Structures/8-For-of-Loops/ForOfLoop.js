/**
 * A for-of loop is used to loop through the values of an array or other iterable objects.
 */

// Example
let colors = ['blue', 'crimson', 'purple'];

for (let color of colors) {
  console.log(color);
}