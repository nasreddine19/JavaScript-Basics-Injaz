/**
 * A do-while loop is similar to a while loop, 
 * but it always runs the code at least once, even if the condition is false.
 */

// Example
let count = 1;

do {
  console.log('Count: ' + count);
  count++;
} while (count <= 5);